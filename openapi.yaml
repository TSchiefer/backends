swagger: "2.0"
info:
  description: "Lists all known DBI backends"
  version: "0.0.1"
  title: "DBI backends"
paths:
  /all:
    get:
      summary: ' All backends'
      responses:
        '200':
          description: OK
        default:
          description: Default response.
      parameters: []
  /by-package:
    get:
      summary: ' Request backend by R package name'
      responses:
        '200':
          description: OK
        '500':
          description: Error (Package name correct?)
        default:
          description: Default response.
      parameters:
      - name: name
        description: Package name
        in: query
        required: no
        schema:
          type: string
